# Ignore all
tests/t_*
benchmarks/*

# Unignore all with extensions
!tests/t_*.*
!benchmarks/*.*

# The compile time benchmarks live in a subdirectory, which needs the same treatment
!benchmarks/vm/
benchmarks/vm/*
!benchmarks/vm/*.*

*.js

.DS_Store
callgrind.out.*
profile_results.txt
docs
*.idx
*.css
